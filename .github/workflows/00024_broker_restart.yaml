name: broker-restart

on: push

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: Slow
        run: |
          sleep 6
          echo "first-done"
  second:
    needs: first
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "second-after-restart"
