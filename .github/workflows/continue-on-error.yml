name: continue-on-error

on: [push, workflow_dispatch]

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: Failing step
        continue-on-error: true
        run: exit 1
      - name: After
        run: echo "after"
