name: env-levels

on: push

env:
  WF: workflow
  SHARED: from-workflow

jobs:
  job:
    runs-on: ubuntu-latest
    env:
      JOB: jobv
      SHARED: from-job
    steps:
      - name: Echo
        env:
          STEP: stepv
        run: echo "wf=$WF job=$JOB step=$STEP shared=$SHARED"
