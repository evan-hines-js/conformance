name: workflow-commands

on: push

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: Annotate
        run: |
          echo "::group::Build details"
          echo "compiling module"
          echo "::endgroup::"
          echo "::warning title=lint::style nit on line 4"
          echo "::notice::build complete"
          echo "::error::non-fatal diagnostic"
          echo "finished annotating"
